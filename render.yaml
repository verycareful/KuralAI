services:
  - type: web
    name: kural-ai
    runtime: docker
    plan: free
    region: singapore
    healthCheckPath: /voices
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
